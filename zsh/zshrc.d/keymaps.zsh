# Option + arrows in most embedded terminals
bindkey "^[b" backward-word
bindkey "^[f" forward-word

# Option + arrows in OOTB iTerm
bindkey "^[[1;3D" backward-word
bindkey "^[[1;3C" forward-word

# Option + delete
bindkey  "^["  backward-delete-word
